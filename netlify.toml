[build]
  command = "npm run build"
  publish = "dist"

# Existing files (including /__forms.html) are served first.
# This catch-all is only for Vue Router paths like /projects/:slug.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
